# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.wrangler
.tanstack

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Million Lint
.million

# vitest
**/__image_diffs__/**
**/__image_snapshots__/**
# library: tmdb response cache from `pnpm library:build` — kept locally
# so re-runs are instant + reproducible; rebuilt on demand. ~30MB so
# not worth committing. The build emits src/data/library.json (small,
# committed) which is what the page actually imports.
data/library-tmdb-cache.json
